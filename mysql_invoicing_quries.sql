CREATE DATABASE `sql_store`

USE `sql_store`

SELECT * FROM `order_item_notes`

-- using the store db.

USE `store`

SELECT `first_name`, `last_name`, `state`  FROM `customers` ORDER BY `customer_id`  
